{
  "name": "project-root",
  "version": "1.0.0",
  "scripts": {
    "build": "curl -sS https://getcomposer.org/installer | php && php composer.phar install --prefix finance-app --no-dev --prefer-dist && npm install --prefix finance-app && npm run build --prefix finance-app"
  }
}
